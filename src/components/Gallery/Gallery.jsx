import Profile, { ProfilesWithAvatars } from '../UserProfile/UserProfile';

const Gallery = () => {
  return (
    <section>
      <Profile />
      <Profile />
      <ProfilesWithAvatars />
    </section>
  );
};

export default Gallery;
